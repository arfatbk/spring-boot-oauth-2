
logging:
  level:
    org.springframework.security: trace

spring:
  application:
    name: resource
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000



server:
  port: 8081